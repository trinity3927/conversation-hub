# Development Log — 2026-03-16

This is the public-safe summary of the main work completed during the initial buildout.

## Milestones

- Bootstrapped the Python package and CLI entrypoint
- Added the normalized conversation schema:
  - `Participant`
  - `ContentPart`
  - `Message`
  - `Conversation`
  - `normalize_timestamp()`
- Added import support for:
  - ChatGPT exports
  - Claude exports
  - Codex local session state
- Added the shared import pipeline and normalized JSON serialization/loading
- Added the `analyze` CLI and deterministic local analysis pipeline
- Added SQLite export and SQLite search
- Added the interactive browse workflow and the redesigned home/browser UI

## Product direction captured

- Local-first conversation ETL and memory extraction tool
- CLI-first MVP
- Durable normalized schema as the core boundary
- SQLite as the default local storage path
- Focus on useful local analysis and search before more advanced storage/search features

## Public repo notes

- Local export folders and generated app data are ignored by git
- Machine-specific paths, internal test harness references, and private setup history were removed from tracked docs/logs
- The repo is intended to be runnable with standard local Python tooling:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
PYTHONPATH=src python -m pytest
```

## Verification snapshot

- Core CLI, connectors, pipelines, storage, and interactive workflow are covered by tests
- Latest full-suite result during cleanup:

```text
34 passed in 0.11s
```
